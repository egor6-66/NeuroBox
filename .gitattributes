# Разработка идёт на Windows, исполнение — в Linux-контейнерах. Без нормализации скрипт с CRLF
# внутри образа падает с «файл не найден», и причина не видна ни в диффе, ни в логе сборки.
* text=auto eol=lf
