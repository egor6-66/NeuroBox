"""Живость сервиса.

Отвечает ровно на один вопрос: процесс поднялся и обрабатывает запросы. НЕ отвечает на
вопрос, готов ли сервис работать — готовность появится тогда, когда появится, от чего
зависеть (база, очередь, серверы). Пока зависимостей нет, второй ручки не существует:
она врала бы «готов» просто потому, что проверять нечего.
"""

from fastapi import APIRouter

router = APIRouter(tags=["meta"])


@router.get("/health")
def health() -> dict[str, str]:
    return {"status": "ok"}
