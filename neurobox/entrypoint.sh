#!/bin/sh
# Схема приводится в порядок ДО того, как сервис начнёт отвечать. Иначе первый же запрос
# приходит на базу, которой код уже не соответствует, и ошибка выглядит случайной.
set -e

alembic upgrade head
exec "$@"
