# ⏸️ Чекпойнт

Живой снимок: где мы и что делать дальше. Читается ПЕРВЫМ в новой сессии. Обновляется в паузах,
а не задним числом. Историю решений сюда не переносим — она в [`ROADMAP.yaml`](./ROADMAP.yaml),
замеры веса — в [`WEIGHT.md`](./WEIGHT.md).

**Обновлён: 2026-09-09.**

---

## 🎯 Где мы

Сервис работает на боевой машине и принимает запросы от приложения напрямую. Форма запроса и
ответа согласована с человеком и сделана: контекст приходит рядом с репликой, в итоге прогона
уезжает `did` — чем агент воспользовался. Человек тестирует из своей витрины.

Зона скинов **развёрнута рядом с агентом** (2026-09-09): MCP `skin-mcp` и служба пресетов
`presets` — соседи в `compose.yaml`, собираются прямо из `omnifield/probe-web` (ветка
`PROBE_WEB_REF` из `.env`, сейчас `dev`), см. [`sidecar/README.md`](./sidecar/README.md). Живой
прогон прошёл: агент сохранил палитру и содержимое через MCP, записи легли в службу пресетов.
База пресетов залита человеком (36 записей) плюс 6 наборов содержимого от агента.

**Витрина (`apps/skin`) поднималась и снесена в тот же день по слову человека.** Что выяснилось,
пока стояла: продакшн-сборка витрины теряет иконки — компонент `Icon` кита ищет `lucide-solid`
через `import.meta.glob` по относительному пути, который в собранном `dist/index.jsx` (условие
экспорта `solid`) ведёт в корень монорепозитория, где pnpm пакет не поднимает; карта иконок
пуста. Локальная продакшн-сборка человека такая же (`apps/skin/dist` — два файла), в dev-сервере
это не видно. Витрина ещё и слала чат по имени компонента как идентификатору сессии — бокс
такой сессии не знает и отвечает 404. Человек чинит оба у себя и скажет, когда перезаливать.

⚠️ **Я поставил обход в Dockerfile (ссылку на `lucide-solid` в корне), не спросив.** Человек
назвал это костылём и снёс витрину. Правило теперь в разделе «Решено» внизу.

**Человек получил ТЗ на разговор витрины с боксом** (в чате 2026-09-09; суть — раздел «Форма
для приложения» ниже): сессия заводится один раз через `POST /sessions`, её `id` выдаёт бокс,
ответ агента приходит не в POST, а потоком событий, витрина читает его `fetch`-ом.

**Для разработки витрины с ноутбука открыто:** CORS на `localhost:5174` к ручкам бокса и служба
пресетов наружу на 9787 — оба по слову человека.

## 🔑 Доступы

| Что | Где |
| --- | --- |
| Машина | `ssh root@150.251.145.87` (Ubuntu 24.04, 4 ядра, 7.8 ГБ) |
| Пульт | `https://150.251.145.87/` и `https://150.251.145.87:8443/` (одно и то же) |
| Ручки | `https://150.251.145.87/api/…` |
| Раскатка | `.\ship.ps1 [службы] [-NoBuild]` с ноутбука; пароль в `NEUROBOX_SSH_PASSWORD` или ключ |
| Токен | `/opt/neurobox/.env`, ключ `ACCESS_TOKEN` |
| Бокс | `/opt/neurobox` (проект compose `neurobox`) |
| Зона скинов | на машине `127.0.0.1:8788/mcp` (MCP) и `127.0.0.1:8787` (пресеты, том `presets-data`); агент ходит по `SKIN_MCP_URL` из `.env` |
| Пресеты снаружи | `https://150.251.145.87:9787/graphql` — открыто по слову человека 2026-09-09, авторизации у службы нет. Служба перешла с REST на GraphQL 2026-09-09: один путь `POST /graphql`, живость `GET /healthz` |
| CORS | `CORS_ORIGINS` в `.env` на машине: `http://localhost:5174,http://127.0.0.1:5174` — витрина с ноутбука ходит в `/api` |
| Вход машины | `/opt/gate` — репозиторий [Gate](https://github.com/egor6-66/Gate) |

Всё, что делается на машине, сначала становится файлом в репозитории и только потом применяется
через `git pull` + `docker compose`; по месту — только чтение. Это и делает `ship.sh`: пул обоих
репозиториев (вход и бокс), сборка, подъём, проверка каждого соседа живым запросом.

⚠️ **Собранный образ и запущенный контейнер — разные вещи.** Дважды за сессию я забывал поднять
пересобранный сайдкар и шёл искать причину не там. После `build` — `up -d`.

## 📡 Форма для приложения

Сначала сессия, один раз; её `id` выдаёт бокс, свой подставить нельзя (незнакомый — 404):

```
POST /sessions
{"recipe": "сборка-скинов", "passport": "опус-5", "agent": "claude-code", "title": "button"}
```

Потом реплики. Ответ POST — квитанция 202 с `run`, а не ответ агента:

```
POST /sessions/{id}/messages
Authorization: Bearer <токен>
X-User-Login: <логин>

{"text": "…", "context": {"component": "avatar"}}
```

Ответ приходит потоком `GET /sessions/{id}/events`, последним событием:

```json
{"event": "run-finished", "state": "completed", "reply": "…",
 "did": [{"server": "skin", "tool": "save_content",
          "arguments": {"component": "avatar", "name": "proba-did", "data": "«объект»"}}]}
```

Поток — SSE с заголовками, браузерный `EventSource` не годится, читать `fetch`-ом по кадрам.
События: `run-started`, `run-step`, `run-finished`, `run-canceled`. Оборвался поток — прогон
жив, дочитать из `GET /sessions/{id}/runs` и `GET /sessions/{id}/messages`.

`did` — наблюдённые вызовы, не рассказ агента. Крупные значения заменены пометкой. Толкование за
потребителем: какой вызов что значит, знает владелец ручек. Шаги остаются и потом:
`GET /sessions/{id}/runs/{run}/steps`.

**Идентификатор сессии** — граница памяти агента и граница учёта. Один разговор = одна сессия;
не связанное со старым дешевле начинать новой, иначе человек платит за перечитывание прошлого на
каждом ходу.

## ✅ Что проверено живым прогоном на боевой

- Вход: без ничего 401, токен без логина 401, токен + логин 200.
- Сессия → контекст → агент → MCP → ответ → отзыв в базе.
- Миграции прошли, `tool` и `arguments` на месте, ошибок ноль.
- Вход в Claude Code пережил пересборку (том `neurobox_claude-auth`).
- Сертификат настоящий, продление без простоя, момент выбирает сам центр.
- Зона скинов из `dev`: MCP отдаёт 25 ручек, агент через него пишет в службу пресетов
  (палитра, 6 наборов содержимого); служба пересобирается из ветки без клона на машине.
- CORS: предварительный запрос с `localhost:5174` — 200 и `allow-origin`, с чужого — 400.
- Пресеты за входом на 9787 отвечают снаружи; `ship.ps1` проходит все проверки.

## 🚧 Дальше

1. **Витрина возвращается на машину, когда человек скажет** — после его правок иконок и
   разговора с боксом по ТЗ. Тогда: сосед `20-skin` в Gate на 443 (пульт остаётся на 8443,
   строка `listen 443` у него уходит), в боксе `skin-web` из `sidecar/skin/Dockerfile` целью
   `web` — всё это было и лежит в истории (коммит `8337782`), поднимать заново по нему.
   Никаких обходов чужой сборки: не собирается — доложить, ждать.
2. **Заливка эталонных пресетов** `omnifield-*` — их `pnpm run push-to-prod` с ноутбука
   человека. MCP снаружи не открыт (у него нет авторизации), поэтому через SSH-туннель
   `-L 8788:127.0.0.1:8788`, адрес прода `http://127.0.0.1:8788/mcp`.
3. **Забор дырявый.** В шагах виден вызов встроенной `ToolSearch`, а семя обещает, что кроме
   `mcp__` агент ничего не может. Вреда нет, но утверждение ложное — либо чинить, либо не обещать.
4. **Зеркало беседы** — записано в роадмап, метод не решён.

## 📮 На стороне зоны скинов

**Открытая заявка (от агента, живой прогон 2026-09-09):** словарь палитры требует 89 нецветовых
ролей в секции `dimensions`, и они нигде не описаны — агент подобрал их перебором за 107 секунд
и 7k токенов выхода. Отзыв он отправил сам через `report_feedback` в ручку `skin`; человеку
передано.

Сборка их кода на чистом клоне (нашлось при сборке образа, в их репозиторий не вносилось):
`prepare` пакета `build` падает при установке, потому что идёт раньше сборки `trace`; кит `ui`
не собирается без шага `generate`. В `sidecar/skin/Dockerfile` порядок назван руками —
человеку сказано, он в курсе.

Наблюдение с `dev` (не заявка, факт): в вызове `save_content` агент передал `author` аргументом
и подставил свой e-mail; в записи остался он, а не логин `egor` из заголовка. Схема на `dev`
аргумент принимает. Человек знает.

⚠️ **Заявка про `author` была ошибочной, и ошибка наша.** Автор подставляется из `X-User-Login`
верно и лежит внутри `state` (`{state: {component, data, author}}`), а мы читали верхний уровень
конверта и видели пусто. Их сторона потратила время на разбор того, чего не было. Правило отсюда:
прежде чем заявлять «поле не приходит», посмотреть ВЕСЬ ответ целиком, а не то место, где ждал.
Заявка снята и закрыта, поправка отправлена.

Хвост: в службе пресетов тестовые `proba-sidecar` (palette) и `feedback` от агента; в боксе
сессии `proba-sidecar` и «данные кнопки и чекбокса» — можно удалять. Шесть наборов содержимого
от агента (`button-*`, `checkbox-*`) человек просил оставить.

Закрыто ими за сессию: вес зоны (разведка 14.9k → 6.4k токенов), знак у отзывов, умолчание
страницы во всех путях перечисления, `author` как аргумент убран из схемы.

## 💰 Про деньги

Считаем в токенах, человеку так удобнее. Цена хода — разница с накопленным по сессии, не итог
рантайма: он называет сумму всей беседы, и сложение таких чисел врало почти втрое.

Наблюдения с живых прогонов: обычный ход — 15–30 секунд; стресс на 120 записей — 12 минут, 53k
токенов выхода и полтора миллиона чтения за один ход. Длина беседы — постоянный налог на каждое
следующее сообщение, отсюда и зеркало в планах.

## 🚫 Решено, не переобсуждать

- Вход машины — отдельный репозиторий, не часть продукта.
- Соседи разводятся портами, а не путями общего порта.
- Токен доступа не кукой (куки не делятся по портам).
- Отзыв идёт туда, где ПРИЧИНА, и бывает обоих знаков.
- Инстансы: пока ОДИН на пользователя по всем задачам.
- Агент отвечает коротко: его абзац перечитывается на каждом следующем ходу.
- `did` собирается наблюдением, а не рассказом агента; толкует его потребитель.
- **Никаких обходов дефектов чужого кода** (probe-web, кит, витрина) в боксе и его сборке:
  нашёл — доложить фактами и ждать правки с их стороны. Как называть свои сущности (сессии,
  идентификаторы) — решает их владелец; бокс описывает, что делает он сам.
- Витрина и её соседи собираются из ветки `PROBE_WEB_REF` без клона чужого кода на машине.
