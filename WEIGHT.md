# ⚖️ Вес

Журнал замеров: сколько контекста съедают MCP-серверы, с которыми работают агенты. Ведётся по
датам, новое сверху. Нужен для оптимизации — по одной точке не видно ничего, по ряду видно всё.

Мерим **ответы**, а не описания. Урок, из которого журнал и завёлся: сервер можно похвалить за то,
как он себя описывает, и не заметить, что одна его ручка отдаёт половину репозитория. Описание
читается глазами, ответ — нет.

---

## Как мерить, чтобы числа были сравнимы

- **Знаки** — длина текста ответа (`content[].text`), без конверта JSON-RPC. Токены — грубо
  `знаки / 3`, для кириллицы это ближе к правде, чем деление на 4.
- **Описания** — имя + описание + схема входа каждой ручки, плюс `instructions` сервера. Это
  постоянная плата: въезжает в контекст на КАЖДОМ прогоне, до первого слова агента.
- **Сценарий** — не одна ручка, а тот путь, который сервер сам советует в инструкции. Смотрим на
  самый тяжёлый разумный, не на удачный.
- **Данные живые.** На пустой базе любой сервер выглядит лёгким.

Контекст между сообщениями не переживает: у каждого прогона свой процесс CLI, и описания едут
заново каждый раз. Поэтому постоянная плата умножается на число сообщений, а не платится однажды.

---

## 2026-09-08 (ночь, 2) — умолчание страницы: починена одна ветка из трёх

Постоянная плата без изменений: 10 413 знаков (≈3 471 токен).

| Вызов | Знаков | Записей | Было |
| --- | ------: | ------: | ---: |
| `list_presets()` | 3 367 | form обрезан до 10 | 7 179 |
| `list_presets(kind=form)` | 5 932 | 28 (все) | — |
| `list_components()` | 8 271 | 35 (все) | 8 271 |
| `list_components(limit=10)` | 2 459 | 10 + курсор | — |
| `list_feedback(status=all)` | 3 429 | 12 заголовков | 3 158 |

Умолчание появилось только в ветке БЕЗ `kind`. Достаточно назвать вид — и потолок исчезает; то же
у `list_components` без аргументов. Курсор в ответе появляется, только если `limit` передан явно,
то есть агент, не знающий про потолок, не узнает и о том, что выдача неполная.

**Сценарий разведки:** 7 563 токена «как получится» (3 471 + `list_components()` 2 757 +
`get_passport` 1 335) против 5 626, если агент сам передаст `limit=10`. Разница целиком в том,
догадается он или нет.

---

## 2026-09-08 (ночь) — `web-core-skin`, 25 ручек, после правок по отзывам

Перемер после трёх заявок. Постоянная плата: инструкция 423 + описания 10 000 = **10 423 знака**
(≈3 474 токена); прибавка от новой ручки `get_feedback`.

| Вызов | Знаков | Было | ≈ токенов |
| --- | ------: | ---: | --------: |
| `list_feedback(status=all)` | 3 158 | 7 507 | 1 052 |
| `list_presets(limit=5)` | 2 307 | недоступно без `kind` | 769 |
| `list_presets()` | 7 179 | 7 179 | 2 393 |
| `list_components()` | 8 271 | 8 271 | 2 757 |
| `get_passport(accordion)` | 4 005 | 4 005 | 1 335 |

**Закрыто:**

- `list_feedback` отдаёт только заголовки, тела уехали в новую `get_feedback`. Вчетверо легче, и
  теперь это перечень, а не выгрузка: на 11 заявках 3 158 знаков против 7 507 на восьми.
- Предельное целое `9007199254740991` убрано из схем: у `limit` стоит `maximum: 100`. Заодно
  закрыт вызов с миллионом.
- Курсор и `limit` у `list_presets` работают без `kind` — раньше пагинация была недоступна именно
  в первом вызове.

**Осталось:** у `list_presets` нет умолчания для `limit`. Описание обещает «первую страницу
каждого из пяти», но без аргумента страница равна всему набору — 7 179 знаков и все 28 форм,
столько же, сколько до правки. С `limit=5` — 2 307. Обещание держится, только если агент сам
догадается передать limit.

---

## 2026-09-08 (вечер) — `web-core-skin`, 24 ручки, порт 8788

Переделка зоны. Мерено на живых данных, тем же способом, что и базовый замер.

**Постоянная плата (каждый прогон):**

| Что | Знаков | Было | ≈ токенов |
| --- | ------: | ---: | --------: |
| Инструкция сервера | 423 | 1 650 | 141 |
| Описания ручек | 9 749 (24 шт.) | 9 315 (13 шт.) | 3 250 |
| **Итого** | **10 172** | 10 965 | **3 391** |

Число ручек почти удвоилось, а плата снизилась: описания резали осознанно. `save_preset`
1809 → 876.

**Ответы читающих ручек:**

| Вызов | Знаков | Было | ≈ токенов |
| --- | ------: | ---: | --------: |
| `list_components()` | 8 271 | 17 358 | 2 757 |
| `list_components(group=actions)` | 288 | — | 96 |
| `list_presets()` | 7 179 | 7 179 | 2 393 |
| `list_presets(kind=form, limit=5)` | 1 082 | — | 360 |
| `list_feedback(status=all)` | 7 507 | — | 2 502 |
| `get_passport(accordion)` | 4 005 | 16 354 | 1 335 |
| `get_io_schema(accordion)` | 1 582 | — | 527 |
| `get_assemblies(accordion)` | 280 | — | 93 |
| `get_doc(forms)` | 1 545 | — | 515 |
| `list_docs()` | 253 | — | 84 |
| `list_content()` | 721 | — | 240 |

Паспорт разделён на три сущности: собственно паспорт, io-схема, сборки. Вместе 5 867 против
16 354 — то есть из ответа ушло около десяти тысяч знаков, а за оставшееся платит тот, кому оно
нужно.

**Сценарий разведки** («список → паспорт», как советует инструкция):

| Путь | ≈ токенов | Было |
| --- | --------: | ---: |
| Без фильтра | 7 483 | 14 892 |
| С фильтром по группе | 4 822 | — |

**Что осталось тяжёлым:**

- `list_presets()` без `kind` — 7 179 знаков, ровно как раньше. Курсор работает ТОЛЬКО вместе с
  `kind`, то есть дешёвый путь есть, а дорогой остаётся по умолчанию. Тот же капкан, из которого
  вылез `list_components`.
- `list_feedback` отдаёт содержимое заявок целиком: 7 507 знаков на восемь штук. Растёт линейно с
  числом заявок, а заявки копятся по построению.
- В схемах у `limit` стоит `maximum: 9007199254740991` — предельное целое, утёкшее в схему. На
  вес почти не влияет, но приезжает агенту в каждом описании как шум.

Сделано верно и стоит назвать: `assemble_preview` отдаёт CSS ссылкой на ресурс, а не текстом в
ответе; `report_feedback` получил знак (`sign: issue|praise`) — похвала принята и помечена 👍.
Три наших отзыва от 2026-09-07 на месте, видны через `list_feedback(status=all)`.

---

## 2026-09-08 — `web-core-skin`, 13 ручек (базовый замер)

Первый замер, точка отсчёта. Снят до переделки, о которой человек предупредил.

**Постоянная плата (каждый прогон):**

| Что | Знаков | ≈ токенов |
| --- | ------: | --------: |
| Инструкция сервера | 1 650 | 550 |
| Описания 13 ручек | 9 315 | 3 105 |
| **Итого** | **10 965** | **3 655** |

Самые тяжёлые описания: `save_preset` 1809, `check_form` 1111, `report_feedback` 956.

**Ответы читающих ручек:**

| Вызов | Знаков | ≈ токенов |
| --- | ------: | --------: |
| `list_components()` | 17 358 | 5 786 |
| `get_passport(accordion)` | 16 354 | 5 451 |
| `list_presets()` | 7 179 | 2 393 |
| `get_passport(button)` | 6 321 | 2 107 |

**Сценарий разведки**, который советует сама инструкция сервера («list_components → get_passport
конкретного компонента, до того как писать»):

| Шаг | ≈ токенов |
| --- | --------: |
| Описания + инструкция | 3 655 |
| `list_components()` | 5 786 |
| `get_passport(accordion)` | 5 451 |
| **До первого действия агента** | **≈ 14 900** |

Отзывы по этому замеру ушли в `report_feedback` сервера 2026-09-07: у `list_components` нет ни
фильтра, ни пагинации, хотя у `list_presets` пагинация есть.

Оговорка к дате: на смотринах утром 2026-09-08 сервер показывал 22 ручки и ~19.5k знаков
описаний, а к вечеру на `127.0.0.1:3000` снова отвечала сборка с 13 ручками. Замер выше снят с
той, что реально отвечала.
